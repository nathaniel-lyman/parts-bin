---
category: Data Display
---

Horizontal bar with leading/trailing slots for controls.

**When to use:** Grouping actions, filters, or search above content.

**Key props:** `children`, `leading`, `trailing`

**Example:**
```tsx
<Toolbar leading={<Input placeholder="Search" />} trailing={<Button>New</Button>} />
```
