---
category: Maps
---

Map plotting sized bubbles at point locations by a metric.

**When to use:** Showing magnitude at specific points (cities, sites).

**Key props:** `points`, `regions`, `features`, `outlinePath`, `selectedPointId`, `onPointSelect`, `valueLabel`

**Example:**
```tsx
<BubbleMap points={points} onPointSelect={select} />
```
