---
category: Data Display
---

Row of removable filter chips plus a clear-all control.

**When to use:** Showing the currently active filters so the user can drop them.

**Key props:** `filters`, `onClearAll`, `emptyLabel`, `className`

**Example:**
```tsx
<AppliedFiltersBar filters={chips} onClearAll={clearAll} />
```
