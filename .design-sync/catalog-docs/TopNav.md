---
category: Shell & Layout
---

Top app bar with breadcrumbs, title, and actions slot.

**When to use:** The header band above page content inside AppShell.

**Key props:** `breadcrumbs`, `title`, `actions`

**Example:**
```tsx
<TopNav title="Projects" actions={<Button>New</Button>} />
```
