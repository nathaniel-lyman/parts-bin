---
category: Primitives
---

Surface container with optional title, description, actions, and footer.

**When to use:** Grouping related content into a bordered panel.

**Key props:** `title`, `description`, `actions`, `children`, `footer`

**Example:**
```tsx
<Card title="Overview" actions={<Button>Edit</Button>}>{body}</Card>
```
