---
category: Data Display
---

Small count badge (caps at 9+) for unread/pending items.

**When to use:** Overlaying a numeric count on an icon or nav item.

**Key props:** `count`

**vs StatusBadge:** Use StatusBadge for a status tone label, not a count.

**Example:**
```tsx
<NotificationBadge count={12} />
```
