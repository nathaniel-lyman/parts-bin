---
category: Charts
---

Themed tooltip body: label, color-keyed rows, optional footer.

**When to use:** Rendering a custom chart tooltip via Recharts content prop.

**Key props:** `label`, `rows`, `footer`

**Example:**
```tsx
<ChartTooltipContent label="Jan" rows={[{ label: 'Value', value: '$78k' }]} />
```
