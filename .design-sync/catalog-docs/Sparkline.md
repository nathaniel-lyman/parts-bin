---
category: Data Display
---

Tiny inline trend line for a numeric series.

**When to use:** A minimal trend indicator inside a card, cell, or KPI.

**Key props:** `data`, `neg`, `width`, `height`

**Example:**
```tsx
<Sparkline data={[1, 3, 2, 5, 4]} />
```
