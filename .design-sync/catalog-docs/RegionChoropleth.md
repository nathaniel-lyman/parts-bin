---
category: Maps
---

Choropleth map shading regions by a metric value.

**When to use:** Comparing a metric across geographic regions by fill intensity.

**Key props:** `regions`, `features`, `selectedRegionId`, `onRegionSelect`, `valueLabel`

**Example:**
```tsx
<RegionChoropleth regions={regions} onRegionSelect={select} />
```
