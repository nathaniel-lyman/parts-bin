---
category: Data Display
---

Avatar + name chip for an assigned person, optionally removable.

**When to use:** Showing (and optionally clearing) the person assigned to something.

**Key props:** `name`, `src`, `status`, `meta`, `onRemove`

**Example:**
```tsx
<AssigneeChip name="Avery Cohen" onRemove={unassign} />
```
