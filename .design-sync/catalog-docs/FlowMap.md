---
category: Maps
---

Map drawing weighted flows between origin/destination points.

**When to use:** Visualizing movement or relationships between places.

**Key props:** `flows`, `regions`, `features`, `outlinePath`, `selectedFlowId`, `onFlowSelect`, `valueLabel`

**Example:**
```tsx
<FlowMap flows={flows} onFlowSelect={select} />
```
