---
category: Feedback
---

Three bouncing dots loading indicator.

**When to use:** Minimal inline loading hint (e.g. pending text).

**Key props:** `label`, `className`

**Example:**
```tsx
<LoadingDots label="Loading" />
```
