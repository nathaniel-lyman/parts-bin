---
category: Feedback
---

Determinate progress bar for an import/upload with label and detail.

**When to use:** Showing measurable progress of a running import or upload.

**Key props:** `value`, `label`, `detail`, `className`

**Example:**
```tsx
<ImportProgress value={62} label="Importing" detail="124 of 200 rows" />
```
