---
category: Shell & Layout
---

Section-level heading with title, description, and actions.

**When to use:** Introducing a section within a page (below PageHeader).

**Key props:** `title`, `description`, `actions`

**Example:**
```tsx
<SectionHeader title="Open requests" actions={<Button>Export</Button>} />
```
