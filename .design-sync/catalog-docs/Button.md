---
category: Primitives
---

Primary action button with intent variants and a loading state.

**When to use:** Any clickable action; pick a variant to signal intent.

**Key props:** `variant`, `size`, `loading`, `disabled`, `onClick`

**Example:**
```tsx
<Button variant="primary" onClick={save}>Save</Button>
```
