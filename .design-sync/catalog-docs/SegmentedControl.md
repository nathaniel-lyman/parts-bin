---
category: Forms
---

Inline single-choice control rendering options as joined segments.

**When to use:** A small set of mutually exclusive view/filter options shown inline.

**Key props:** `options`, `value`, `onValueChange`, `size`, `label`

**Example:**
```tsx
<SegmentedControl options={opts} value={v} onValueChange={setV} />
```
