---
category: Data Display
---

Overlapping stack of avatars with a +N overflow chip.

**When to use:** Showing several people compactly (e.g. assignees on a row).

**Key props:** `users`, `max`, `size`, `className`

**Example:**
```tsx
<AvatarGroup users={people} max={4} />
```
