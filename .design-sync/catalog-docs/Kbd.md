---
category: Primitives
---

Keyboard-key chip for shortcut hints; renders semantic <kbd> elements.

**When to use:** Showing a keyboard shortcut next to a command, menu item, or in help text.

**Key props:** `keys`, `children`, `className`

**Example:**
```tsx
<Kbd keys={['Ctrl', 'K']} />
```
