import { FlowMap } from 'parts-bin'

export function PipelineFlows() {
  return (
    <div style={{ width: 520 }}>
      <FlowMap valueLabel="pipeline ($k)" ariaLabel="Cross-region expansion pipeline" />
    </div>
  )
}
